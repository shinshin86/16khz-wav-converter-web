# 16kHz wav converter web

Converter with WebUI to convert audio files to 16kHz wav files.
Created using ffmpeg.wasm.

## Usage

Start local server. Then access port `localhost:3000`.

```sh
yarn dev
```